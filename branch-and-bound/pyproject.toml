[tool.poetry]

package-mode = false

[tool.poetry.dependencies]
python = "<3.13,>=3.9"
numpy = "1.26.4"
networkx = "3.2.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
